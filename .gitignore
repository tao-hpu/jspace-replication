# local-only working notes (non-public)
CLAUDE.md
docs/_*
experiments/_*
results/_*
_RESUME.md
.env

# writing workspace (article / paper drafts) stays local; the repo ships
# only the engineering needed to reproduce the experiments
paper/

.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store

# upstream reference repo lives as a shallow clone, not committed
# (pinned commit recorded in third_party/PINNED_COMMIT.txt)
third_party/jacobian-lens/

# scratch experiment logs (local-only, not for the public repo)
scratch_logs/

# large artifacts: lens weights, model caches
*.pt
*.safetensors
out/
results/**/*.json.gz
