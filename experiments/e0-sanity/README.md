# E0 — Pipeline sanity

Fit and apply a Jacobian lens on GPT-2 124M and reproduce a walkthrough-level
slice page (qualitative only; a base model can't run the conversational
experiments). Goal: verify the full fit → apply → visualize pipeline on the
cheapest possible model, and locate its workspace band.

Milestone M1. Output: `results/figures/e0-*`, log entry.
